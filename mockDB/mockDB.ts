import { IUser } from "../utils/interfaces";

const mockDB: IUser[] = [
  {
    name: 'Fake Namerson',
    email: 'FNamerson@domain.com',
    budgets: [],
    expenses: []
  }
] 

export default mockDB;